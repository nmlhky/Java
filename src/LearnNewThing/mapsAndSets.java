package questionsAnswers.LearnNewThing;

import java.util.Collections;
import java.util.HashMap;
import java.util.TreeMap;
import java.util.TreeSet;

public class mapsAndSets {
    public static void main(String[] args) {
        // Tree map sorted -not dublicated
        TreeMap<String, Integer> tree = new TreeMap<>(Collections.reverseOrder());
        TreeMap<String, Integer> tree2 = new TreeMap<>();

        // HashMap not sorted;
        HashMap<String ,Integer> map = new HashMap<>();

        // TreeSet -not allowed duplicate -not allowed null -sorted
        TreeSet<Integer>  treesetexampe = new TreeSet<>();

        // HashMap -not sorted

    }
}
